
<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (0..5), as: :radio_buttons, item_wrapper_class: "radio-inline"  %>

  <%= f.submit "add a review", class: "btn btn-primary btn-sm"%>
  <hr>

<% end %>
